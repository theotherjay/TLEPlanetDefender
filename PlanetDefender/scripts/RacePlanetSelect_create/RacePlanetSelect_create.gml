//sets or resets global race and planet and cash
global.playerCash = CASH_STARTING;
global.playerRace = "human"
global.playerPlanetSelectedType = "terrian"

// Used by scr_rm_command_create: 
// The first time rm_command is open, it creates the galaxy map.  
// This should only happen once.
global.rm_command_initialized = false; 

GoogleMobileAds_HideBanner();

