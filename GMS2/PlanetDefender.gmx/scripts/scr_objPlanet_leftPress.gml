//All handled inside scr_DefendPlanetMapController_step now

/*if(!global.sidebar_btnPause_leftPressFlag)
{
    if(global.objPlanetOverlay == PlanetOverlay.Default)
    {
        global.objPlanet_leftPressFlag = true;
    }
}
*/


var a = 1; //to prevent empty script from causing problems until I can remove it.


